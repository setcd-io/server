services:
  etcd:
    build:
      context: .
      dockerfile: Dockerfile
    healthcheck:
      test: ["CMD", "server", "status"]
      interval: 1s
      retries: 1000
    ports:
      - "2379:2379"
    volumes:
      - ./src/certs:/certs
    env_file:
      - ./.cache/dynamodb-remote.env
